#include <iostream>
#include <string>

using namespace std;

int main()

{
    string command;

    while(1 ==1) // temporary ondition for now...
    {
        command.clear();
        cout << "What shall i do? ";
        getline(cin, command);
        cout << "Your raw command was" << command << end1;
    }
    return 0;
}